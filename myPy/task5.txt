def sortArray():
    for y in range(len(numbersArr)):
        for i in range(len(numbersArr) - 1):
            if numbersArr[i] > numbersArr[i + 1]:
                numbersArr[i], numbersArr[i + 1] = numbersArr[i + 1], numbersArr[i]


def median():
    if len(numbersArr) % 2 == 1:
        return [numbersArr[int((len(numbersArr) + 1) / 2 - 1)]]
    else:
        return [(numbersArr[int((len(numbersArr)) / 2)] +
                 numbersArr[int((len(numbersArr)) / 2 - 1)]) / 2]


if __name__ == '__main__':
    numbersArr = []
    while True:
        try:
            number = int(input('enter a number or Enter to finish: '))
            numbersArr.append(number)
            # for i in range(random.randint(5, 10)):
            #    numbersArr.append(random.randint(1, 10))
        except ValueError as err:
            if len(numbersArr) > 0:
                count = len(numbersArr)
                summa = sum(numbersArr)
                lowest = min(numbersArr)
                highest = max(numbersArr)
                print('numbers', numbersArr)
                sortArray()
                m = median()
                print('count =', count,
                      'sum =', summa,
                      'lowest =', lowest,
                      'highest =', highest,
                      'mean =', summa / count,
                      'median =', m)
            break

